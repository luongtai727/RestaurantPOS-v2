package com.example.restaurantpos.ui.adapter

import androidx.fragment.app.Fragment
import androidx.fragment.app.FragmentManager
import androidx.lifecycle.Lifecycle
import androidx.viewpager2.adapter.FragmentStateAdapter

/*
class TapAndViewPager2Adapter  (fragmentManager: FragmentManager, lifecycle: Lifecycle)
    : FragmentStateAdapter(fragmentManager, lifecycle) {


    override fun createFragment(position: Int): Fragment {
        // Dung when chay lan luot cac vi tri: Cho den Return o tren
        return when(position){
            0 -> {Fragment1()}
            1 -> {Fragment2()}
            2 -> {Fragment3()}
            3 -> {Fragment4()}
            else -> {Fragment5()}

        }
    }

    override fun getItemCount(): Int {
        // So Fragment se tra ve
        // Hien co 5 buc anh tren 5 Fragments --> Return 5
        return 5
    }

}*/
